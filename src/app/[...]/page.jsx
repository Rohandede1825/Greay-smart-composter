import React from 'react';
import PropTypes from 'prop-types';

page.propTypes = {
    
};

function page(props) {
    return (
        <div>
            hhh
        </div>
    );
}

export default page;